<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Book Swap" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "login", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header class="header-bar">
      <div class="header-left">
        <%= link_to "Home", root_path, class: "header-link" %>
      </div>
      <div class="header-right">
        <% if authenticated? %>
          <%= button_to "Log out", session_path, method: :delete, class: "logout-button" %>
        <% end %>
      </div>
    </header>

    <main class="main-content">
      <%= yield %>
    </main>

    <footer class="footer-bar">
      <p>&copy; <%= Time.now.year %> Book Swap</p>
    </footer>
  </body>
</html>
